# 🔥 小程序上线后功能失效问题排查

## 问题现象

- ✅ 体验版：功能正常
- ✅ 开发版：功能正常
- ❌ 正式版（上线后）：功能失效

---

## 🎯 最可能的原因：服务器域名未配置 ⭐⭐⭐

### 原因分析

**开发版和体验版为什么能用？**

在微信开发者工具中，可以开启「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」：
- 开发版：默认不校验
- 体验版：可以配置不校验
- **正式版：强制校验** ⚠️

**正式版要求：**
- 必须在微信小程序后台配置服务器域名白名单
- 所有网络请求必须使用 HTTPS
- 域名必须在合法域名列表中

---

## 🔍 快速诊断

### 症状 1：网络请求失败

**表现：**
- 小程序加载正常
- 点击功能按钮无响应
- 控制台报错：`request:fail` 或 `url not in domain list`

**原因：** 服务器域名未配置

---

### 症状 2：API 返回错误

**表现：**
- 网络请求可以发送
- 但返回错误数据或 404

**原因：** 域名配置错误或服务未正确部署

---

## ✅ 解决方案：配置服务器域名 ⭐⭐⭐

### 步骤 1：登录微信小程序后台

```
https://mp.weixin.qq.com/
```

---

### 步骤 2：进入开发管理

1. 登录后，点击左侧菜单「开发」
2. 点击「开发管理」
3. 点击「开发设置」

---

### 步骤 3：找到「服务器域名」

在开发设置页面，找到「服务器域名」区域

---

### 步骤 4：配置 request 合法域名

**点击「request 合法域名」右侧的「修改」**

**添加你的域名：**

#### 选项 A：使用默认域名（如果域名绑定有问题）
```
https://skin-detection-serve-235668-9-1411837125.sh.run.tcloudbase.com
```

#### 选项 B：使用自定义域名（如果已成功配置）
```
https://api.gaodiai.cn
```
或
```
https://skin-api.gaodiai.cn
```

**重要：**
- ⚠️ **必须使用 HTTPS**，不能是 HTTP
- ⚠️ **不要带端口号**
- ⚠️ **不要带路径**，只填域名
- ⚠️ **必须配置主域名，不能是子路径**

---

### 步骤 5：保存并等待生效

1. 点击「保存」
2. 扫描二维码验证（可能需要管理员扫码）
3. 等待配置生效（通常 5-10 分钟）

---

## 🔧 其他可能原因

### 原因 1：环境变量配置问题

**检查 .env.local 配置：**

```bash
# 检查 PROJECT_DOMAIN 是否正确
PROJECT_DOMAIN=https://your-domain.com
```

**检查 config/index.ts：**

```typescript
defineConstants: {
  PROJECT_DOMAIN: JSON.stringify('https://your-domain.com'),
  TARO_ENV: JSON.stringify(process.env.TARO_ENV),
  TARO_APP_WEAPP_APPID: JSON.stringify('wx8826c7b681ec3c65'),
}
```

**确保：**
- 正式版和开发版使用相同的域名
- 域名可以正常访问
- HTTPS 证书有效

---

### 原因 2：云托管服务配置问题

**检查云托管服务状态：**

1. 登录云托管控制台
2. 检查服务 `skin-detection-server` 是否运行正常
3. 检查服务实例是否健康
4. 检查服务日志是否有错误

---

### 原因 3：HTTPS 证书问题

**检查 HTTPS 证书：**

1. 在浏览器中访问你的域名
2. 检查是否有证书错误
3. 检查证书是否过期
4. 检查证书是否信任

**测试 HTTPS：**
```bash
curl https://your-domain.com/api/health
```

---

## 📋 完整配置检查清单

### 服务器域名配置（最重要）

- [ ] ✅ 已登录微信小程序后台
- [ ] ✅ 已进入开发设置
- [ ] ✅ 已配置 request 合法域名
- [ ] ✅ 域名格式正确（HTTPS，无端口号）
- [ ] ✅ 已保存并等待生效
- [ ] ✅ 已重新提交审核和发布

### 域名和网络检查

- [ ] ✅ 域名可以正常访问
- [ ] ✅ HTTPS 证书有效
- [ ] ✅ 可以通过 curl 测试 API
- [ ] ✅ 网络请求配置正确

### 云托管服务检查

- [ ] ✅ 云托管服务运行正常
- [ ] ✅ 服务实例健康
- [ ] ✅ 服务日志无错误
- [ ] ✅ API 接口正常工作

### 小程序配置检查

- [ ] ✅ PROJECT_DOMAIN 配置正确
- [ ] ✅ 网络请求代码使用正确的 URL
- [ ] ✅ 小程序代码已重新编译
- [ ] ✅ 已重新提交审核和发布

---

## 🚀 快速修复流程

### 第 1 步：配置服务器域名（5 分钟）⭐⭐⭐

1. 登录微信小程序后台
2. 开发 → 开发管理 → 开发设置
3. 修改 request 合法域名
4. 添加：`https://skin-detection-serve-235668-9-1411837125.sh.run.tcloudbase.com`
5. 保存并等待生效

---

### 第 2 步：测试域名访问（1 分钟）

在浏览器中访问：
```
https://skin-detection-serve-235668-9-1411837125.sh.run.tcloudbase.com/api/health
```

确认返回正常响应。

---

### 第 3 步：重新提交审核和发布（5-10 分钟）

1. 在微信开发者工具中
2. 点击「上传」
3. 登录微信小程序后台
4. 提交审核
5. 审核通过后发布

---

### 第 4 步：测试正式版（2 分钟）

1. 搜索小程序
2. 打开正式版
3. 测试所有功能
4. 确认功能正常

---

## 💡 预防措施

### 1. 开发环境配置

在开发环境中，可以在开发者工具中设置：
- ✅ 不校验合法域名
- ✅ 方便开发和测试

### 2. 测试环境配置

在测试环境中：
- ✅ 关闭「不校验合法域名」
- ✅ 测试正式环境配置
- ✅ 确保功能正常

### 3. 正式环境配置

在正式环境中：
- ✅ 必须配置服务器域名
- ✅ 必须使用 HTTPS
- ✅ 确保所有网络请求都使用合法域名

---

## ❓ 如何确认问题

### 测试方法 1：查看控制台日志

在微信开发者工具中：
1. 打开「调试器」
2. 查看 Console 标签
3. 查看是否有网络请求错误
4. 查看是否有 `url not in domain list` 错误

### 测试方法 2：使用真机调试

1. 在开发者工具中，点击「真机调试」
2. 扫码连接手机
3. 查看手机上的错误提示
4. 检查网络请求是否成功

### 测试方法 3：使用 vConsole

1. 在正式版小程序中打开 vConsole
2. 查看网络请求日志
3. 查看是否有域名相关错误

---

## 📞 需要帮助？

**如果以上步骤都无法解决问题：**

1. **告诉我具体的错误信息**
   - 控制台的错误日志
   - 网络请求的响应
   - 错误提示的文字

2. **告诉我你目前的配置**
   - 微信小程序后台是否配置了服务器域名？
   - 配置的是什么域名？
   - 域名是否可以正常访问？

3. **告诉我测试结果**
   - 浏览器中访问域名是否正常？
   - curl 测试是否成功？
   - 云托管服务是否正常运行？

我会根据你的具体情况提供针对性的解决方案！

---

## 🎯 现在就开始修复

### 立即操作（5 分钟）：

1. 登录微信小程序后台
2. 开发 → 开发管理 → 开发设置
3. 修改 request 合法域名
4. 添加：`https://skin-detection-serve-235668-9-1411837125.sh.run.tcloudbase.com`
5. 保存并等待生效
6. 重新提交审核和发布

### 如果自定义域名配置成功：

使用自定义域名：
```
https://api.gaodiai.cn
```
或
```
https://skin-api.gaodiai.cn
```

---

**现在就去配置服务器域名吧！** 🚀

完成配置后告诉我结果！
